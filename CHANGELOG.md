# Changelog

## 2026-05-25 — 4K upgrade and cleanup

- chore: add ui helpers and update `next-env.d.ts` (commit `663fecd`)
- chore: ignore upgrade artifacts (portfolio-4k-upgrade) (commit `f7fdc01`)
- Apply 4K upgrade from `portfolio-4k-upgrade.zip` and create backups (commit `ab87fd4`)

## 2026-05-24 — prior updates

- Suppress hydration mismatch warnings from Dark Reader extension (commit `6f41f35`)
- Update portfolio components and configuration (commit `67d245d`)

Notes:
- Backups of replaced files are saved in `backup-before-4k-upgrade-*` in the project root.
- The dev server was tested locally and the site serves at `http://localhost:3000`.
